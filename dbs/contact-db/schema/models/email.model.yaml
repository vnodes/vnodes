name: Email

extends:
  - WithId
  - WithTimestamps
  - WithNotes

properties:
  email:
    type: string
    format: email
    required: true

relations:
  contact:
    type: owner
    model: Contact

uniques:
  - [contactId, email]

indexes:
  - [email]
  - [contactId]
