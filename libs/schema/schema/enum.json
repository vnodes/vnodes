{
    "$schema": "https://json-schema.org/draft-07/schema#",
    "title": "Enum",
    "$id": "enum",
    "description": "Scheam to design enum models",
    "properties": {
        "name": {
            "type": "string"
        },
        "values": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "minItems": 2
        }
    },
    "required": [
        "name",
        "values"
    ]
}