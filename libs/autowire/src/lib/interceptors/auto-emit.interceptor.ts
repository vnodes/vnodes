// - [ ] create auto emit interceptor that emit payload if the operation is marked by Emit decorator

// import { NestInterceptor } from '@nestjs/common';

// export class AutoEmitInterceptor implements NestInterceptor {
//     intercept() {
//         return;
//     }
// }
