export type {
    DiskStorageOptions,
    ErrorCode,
    Field,
    FileFilterCallback,
    Multer,
    MulterError,
    Options,
    StorageEngine,
} from 'multer';

export { diskStorage, memoryStorage } from 'multer';
