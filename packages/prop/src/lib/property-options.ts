export type PropertyOptions = {
  // - [ ] Create options for each value type such as StringPropertyOptions, NumberPropertyOptions, BooleanPropertyOptions etc... Add all value types.
  type?: string;
};
